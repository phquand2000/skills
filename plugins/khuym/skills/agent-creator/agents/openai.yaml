interface:
  display_name: "Agent Creator"
  short_description: "Scaffold repo-local Codex agents"
  default_prompt: "Use $agent-creator to add a new persistent agent under .codex/agents for the current repo, following the existing local conventions and wiring any needed skills."
